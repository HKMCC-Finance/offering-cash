# Core - needed by both the cash app and the check scanner
openpyxl
pdfplumber
pandas
pyautogui
pyinstaller

# Check scanning (OCR)
opencv-python
tqdm
torch
easyocr
transformers

# Default check-reading backend (--backend qwen-vl): a vision-language model
# run locally on the GPU. Needs a CUDA build of torch - the plain PyPI wheel is
# CPU-only on Windows and cannot use the card no matter what hardware is
# present. On this machine:
#   pip install --index-url https://download.pytorch.org/whl/cu126 torch==2.8.0+cu126 torchvision==0.23.0+cu126
accelerate
# Weights (~8 GB, downloaded once to the HuggingFace cache):
#   Qwen/Qwen2.5-VL-3B-Instruct
# Override with the OFFERING_VLM_MODEL environment variable.

# Optional alternative backend - install only if benchmarking it.
#   paddleocr        # --backend paddleocr

# word2number was removed: the legal-amount parser in src/check_fields.py
# replaces it. It raised on any unexpected token, which forced the old
# "shrink the ROI until it parses" retry loop that accepted partial matches.
